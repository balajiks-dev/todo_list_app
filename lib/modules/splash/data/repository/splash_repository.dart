class SplashRepository {
  // Future<MetaResponse> userLogin(SignInRequestModel signInRequestModel) async {
  //   final MetaResponse m = await BaseAPIServices().processPostURL(
  //     URLUtils().getLoginUrl(),
  //     signInRequestModel.toJson(),
  //   );
  //   return m;
  // }
}
